<%= link_to gravatar_for(current_user, size: UsersHelper::GRAVATAR_USER_INFO_SIZE), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_profile"), current_user %></span>
<span><%= t(".micropost_count", count: current_user.microposts.count) %></span>
